$('#book-search-results').html('<%= j render 'books/search_results.html' %>')

// Set listeners to link results to book page
$('li[data-link]').on('click', function (e) {
    window.location = $(this).data('link')
})
